package spil;
public class GameDriver {
	public static void main(String[] args) {
		//Creates a new instance of the Game object, and runs a game.
		Game diceGame = new Game();
		diceGame.runGame();
	}
}